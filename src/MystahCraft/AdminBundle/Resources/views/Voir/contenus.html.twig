{% extends 'MystahCraftAdminBundle::layout.html.twig' %}

{% block title %}
	{{ parent() }} - Page d'administration des rÃ¨gles
{% endblock %}

{% block contenu %}
	<h1>Page d'administration des règles</h1>
	{% for contenu in contenus %}
	<ul>
		<li>
			<a href="{{ path('admin_modifier_contenu', { id : contenu.id}) }}">Modifier {{ type }}{% if multiple %} n°{{ contenu.id }}{% endif %}</a>
		</li>
	</ul>
	{% endfor %}
	{% if multiple %}
		<a href="{{ path('admin_ajouter_contenu', { type : type }) }}">Ajouter {{ type }}</a>
	{% endif %}
{% endblock %}